<%= form_with(model: humidity_measure, local: true) do |form| %>
  <% if humidity_measure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(humidity_measure.errors.count, "error") %> prohibited this humidity_measure from being saved:</h2>

      <ul>
      <% humidity_measure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :humidity_measure_time %>
  </div>

  <div class="field">
    <%= form.label :measure %>
    <%= form.text_field :measure, id: :humidity_measure_measure %>
  </div>

  <div class="field">
    <%= form.label :device_id %>
    <%= form.text_field :device_id, id: :humidity_measure_device_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
