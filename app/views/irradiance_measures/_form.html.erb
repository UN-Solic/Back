<%= form_with(model: irradiance_measure, local: true) do |form| %>
  <% if irradiance_measure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(irradiance_measure.errors.count, "error") %> prohibited this irradiance_measure from being saved:</h2>

      <ul>
      <% irradiance_measure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
