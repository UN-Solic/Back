<%= form_with(model: temp_measure, local: true) do |form| %>
  <% if temp_measure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(temp_measure.errors.count, "error") %> prohibited this temp_measure from being saved:</h2>

      <ul>
      <% temp_measure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :temp_measure_time %>
  </div>

  <div class="field">
    <%= form.label :measure %>
    <%= form.text_field :measure, id: :temp_measure_measure %>
  </div>

  <div class="field">
    <%= form.label :device_id %>
    <%= form.text_field :device_id, id: :temp_measure_device_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
